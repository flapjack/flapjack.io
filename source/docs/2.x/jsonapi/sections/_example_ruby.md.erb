<%
ext = if resource_linked_type.nil?
  resource_name
else
  "#{resource_type}_link_#{resource_linked_name}"
end
%>

```ruby
require 'flapjack-diner'
Flapjack::Diner.base_uri('localhost:3081')

<% case method_type
   when :post %>
Flapjack::Diner.create_<%= ext -%>(
  :id => '<%= uuids[0] -%>'
)

# or

Flapjack::Diner.create_<%= ext -%>({
  :id => '<%= uuids[0] -%>'
}, {
  :id => '<%= uuids[1] -%>'
})
<% when :get %>
Flapjack::Diner.<%= ext -%>(
  :fields => {},
  :filter => {},
  :include => [],
  :sort => '',
  :page => 1, :per_page => 20
)
<% when :patch %>
Flapjack::Diner.update_<%= ext -%>(
  :id => '<%= uuids[0] -%>'
)

# or

Flapjack::Diner.update_<%= ext -%>({
  :id => '<%= uuids[0] -%>'
}, {
  :id => '<%= uuids[1] -%>'
})
<% when :delete %>
Flapjack::Diner.delete_<%= ext -%>(
  '<%= uuids[0] %>'
)

# or

Flapjack::Diner.delete_<%= ext -%>(
  '<%= uuids[0] %>',
  '<%= uuids[1] %>'
)
<%
end -%>
```
